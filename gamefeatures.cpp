#include <iostream>
#include <array>
using namespace std;
 
/*-
struct Observer{
    virtual void update()=0;
 
};
 
class Subject{
    vector <Observer *> observers;
    public:
    void registerObserver(Observer *observer) {
        observers.push_back(observer);
    }
    void removerObserver(Observer *observer) {
        remove(begin(observers), end(observers), observer)
    }
    void notifyObservers() const {
        for (auto& observers: observer)
        observer->update();
    }
};
 
*/
 
/*
class GridInspector {   
    vector < vector<bool> > gridExploded;
    vector< vector<Cell> > cells;
 
    public:
    GridInspectror() {}
 
    vector < vector<bool> > InspectCase();
 
    bool IsBombCandy() { return false; };
    bool IsWrappedCandy() { return false; };
    bool IsStripedCandy() { return false; };
 
    void TickExplodedCasesNormalCandy() {};
    void TickExplodedCasesBombCandy() {};
    void TickExplodedCasesWrappedCandy() {};
    void TickExplodedCasesStripedCandy() {};
 
};
 
vector < vector<bool> > GridInspector::InspectCase() {
 
    return gridExploded;
}
 
*/
